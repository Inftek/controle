{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TbCategoria list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Catdescricao</th>
                <th>Catativo</th>
                <th>Catcodigo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('categoria_show', { 'id': entity.catCodigo }) }}">{{ entity.catDescricao }}</a></td>
                <td>{{ entity.catAtivo }}</td>
                <td>{{ entity.catCodigo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('categoria_show', { 'id': entity.catCodigo }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('categoria_edit', { 'id': entity.catCodigo }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('categoria_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
